-- 18-04-2012 Global Redirect
   Due to a redirect bug in the core module we added a dev version of the global redirect module to this project. The bug happened when trying activate localization.
   Unless there is a new version with a fix for this bug, this module should not be removed.
